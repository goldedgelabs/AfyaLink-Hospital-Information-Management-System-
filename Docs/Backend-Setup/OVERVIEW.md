AfyaLink Hybrid Backend — Overview (Developer-Friendly)

System summary
- Backend: NestJS modular structure (Express-compatible). TypeScript/JavaScript. Prisma ORM + PostgreSQL.
- API: REST + GraphQL + WebSocket.
- Integrations: NeuroEdge AI, OpenAI fallback, S3-compatible storage, lab & pharmacy adapters.
- Queue: BullMQ (Redis) for background jobs.
